[project]
name = "f1-stats"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.4.1",
    "httpx>=0.28.1",
    "pyyaml>=6.0.3",
    "tqdm>=4.67.3",
]
